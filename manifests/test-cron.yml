name: Test Cron

on:
  schedule:
    - cron: "* * * * *"

  workflow_dispatch:

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "Cron works"
          date